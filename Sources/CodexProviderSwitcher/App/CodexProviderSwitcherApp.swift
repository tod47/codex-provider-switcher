import SwiftUI

@main
struct CodexProviderSwitcherApp: App {
    var body: some Scene {
        MenuBarExtra(
            "Codex Provider Switcher",
            systemImage: "arrow.triangle.2.circlepath"
        ) {
            Text("Ready")
                .padding()
        }
    }
}
